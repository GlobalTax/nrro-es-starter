

## Plan: SEO Overhaul — Sitemap, Noindex, and Dynamic Meta Tags

This covers all 3 messages from the user in a single implementation pass.

---

### 1. Sitemap Rewrite (`regenerate-sitemap` Edge Function + `public/sitemap.xml`)

**Problem**: Current sitemap includes many URLs that shouldn't be indexed (legal pages, `/es/` landings, news, old routes like `/metodologia`, `/estrategia`, `/orquest-kairoshr`, etc.).

**Changes to `supabase/functions/regenerate-sitemap/index.ts`**:
- Replace `staticRoutes` array with only the approved URLs:
  - Homepage: `/` (no CA/EN alternates for all — only where specified)
  - 25 ES service pages under `/servicios/...`
  - 6 CA service pages under `/ca/serveis/...`
  - 5 EN service pages under `/en/services/...`
  - Static pages: `/contacto`, `/equipo`, `/sobre-nosotros`, `/casos-exito`, `/sectores`, `/recursos`, `/blog`, `/carreras`, `/abogados-herencias-barcelona`, `/canal-denuncias`, `/ca`, `/ca/carreres`, `/en`
- Remove `serviceLandingGroups` entirely (the `/es/...` landings are gone from sitemap)
- Remove news articles section (section 4) completely
- Keep blog posts dynamic section but only `/blog/{slug}` (no `/en/blog/` alternates)
- Remove case studies from sitemap (not in approved list — wait, `/casos-exito` is listed as static but no dynamic case study slugs were listed)
- Remove job positions section
- Remove legal pages (`/privacidad`, `/aviso-legal`, `/cookies`, `/condiciones-contratacion`)
- Hreflang: Only add alternates where the user explicitly provided language variants (most service pages are standalone — no cross-language alternates)
- Regenerate `public/sitemap.xml` as static fallback with matching content

**Note on hreflang**: The user listed CA and EN services separately without linking them as alternates of ES services. They'll be included as standalone URLs without hreflang groups unless they share the same service concept.

---

### 2. Noindex on `/recursos/noticias/*`

**Problem**: There are no public routes matching `/recursos/noticias/*` in the React router. The news pages seem to be admin-only (`/admin/news`). 

**Approach**: Add a check in the inline `index.html` script — if `pathname` starts with `/recursos/noticias`, inject `<meta name="robots" content="noindex, nofollow" />`. This covers any scenario where such URLs might be crawled even without explicit React routes.

Also add the same noindex logic in `Meta.tsx` as a safety net for any component that might render on news-like paths.

---

### 3. Dynamic Meta Tags Script in `index.html`

**Replace the existing inline script** (lines 19-41) with a comprehensive script that:
1. Defines a route map `R` with `t` (title) and `d` (description) for ~35 routes
2. On page load, looks up `window.location.pathname` in the map
3. Updates: `document.title`, `meta[name="description"]`, `meta[property="og:title"]`, `meta[property="og:description"]`, `link[rel="canonical"]`, `meta[property="og:url"]`
4. Creates tags if they don't exist
5. Preserves the existing hreflang/canonical/og:locale logic
6. Adds noindex injection for `/recursos/noticias` paths

The route map includes all 35 routes provided by the user with their exact titles and descriptions.

---

### Files Modified

1. **`index.html`** — Replace inline script (lines 19-41) with the full route-map-based meta tag injector + noindex logic
2. **`supabase/functions/regenerate-sitemap/index.ts`** — Complete rewrite of `staticRoutes`, remove `serviceLandingGroups`, remove news/jobs sections, keep only approved URLs with blog dynamic lookup
3. **`public/sitemap.xml`** — Will be regenerated by the edge function after deployment

### Scope
- 2 files modified, 1 edge function to deploy
- No database changes needed

